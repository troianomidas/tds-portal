@inject DataRequest DataRequest
@inject IJSRuntime JsRuntime

<div class="modal fade" id="modal_new_category" data-bs-backdrop="static" tabindex="-1" role="dialog" data-keyboard="false">
    <div class="modal-dialog modal-dialog-centered mw-650px">
        <div class="modal-content">
            <div class="modal-header pb-0 border-0 justify-content-end">
                <div class="btn btn-sm btn-icon btn-active-color-danger" data-bs-dismiss="modal">
                    <i class="ki-outline ki-cross fs-1"></i>
                </div>
            </div>
            <div class="modal-body scroll-y mx-5 pt-0">
                <div class="mb-10 text-center">
                    <h1 class="mb-3">Nova categoria</h1>
                    <div class="text-gray-600 fw-semibold fs-5">Categorias são usadas para agrupar produtos que são similares em termos de suas características.</div>
                </div>
                <div class="form">
                    <div class="fv-row mb-7 form-floating">
                        <input class="form-control form-control-solid" placeholder="" maxlength="45" name="categoryName" autocomplete="off" @bind="_categoryName" @onkeyup="async (e) => await EnterKeyPressAsync(e.Key)"/>
                        <label class="required">Nome</label>
                    </div>
                    <div class="fv-row mb-7 form-floating">
                        <input class="form-control form-control-solid" placeholder="" maxlength="250" name="categoryDescription" autocomplete="off" @bind="_categoryDescription" @onkeyup="async (e) => await EnterKeyPressAsync(e.Key)"/>
                        <label>Descrição</label>
                    </div>
                    <div class="text-gray-600"></div>
                    <div class="text-center pt-5">
                        <button type="button" class="btn btn-light me-3" @onclick="async () => await CloseAsync()">
                            Cancelar
                        </button>
                        @if (_loading)
                        {
                            <button type="button" class="btn btn-success">
                                Salvando <span class="spinner-border spinner-border-sm align-middle ms-2"></span>
                            </button>
                        }
                        else
                        {
                            <button type="button" class="btn btn-success" @onclick="async () => await SaveAsync()">
                                Salvar categoria
                            </button>
                        }
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@code
{
    [Parameter]
    public EventCallback<bool> OnSave { get; set; }

    private string? _categoryName;
    private string? _categoryDescription;

    private bool _loading;
    private string? _currentMsg;

    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender)
        {
            await JsRuntime.InvokeVoidAsync("OnModalShow.Focus", "modal_new_category", "categoryName");
        }
    }

    async Task SaveAsync()
    {
        _currentMsg = null;
        _loading = true;

        await Task.Delay(TimeSpan.FromSeconds(0.5));
        ApiResponse response = await DataRequest.SendAsync(HttpMethod.Post, "/category", new { Name = _categoryName, Description =_categoryDescription });
        _loading = false;
        if (response.IsSuccessRequest)
        {
            await JsRuntime.InvokeVoidAsync("Toastr.Success", "Categoria criada com sucesso");
        }
        else
        {
            await JsRuntime.InvokeVoidAsync("Toastr.Warning", response.Error);
            return;
        }

        await CloseAsync();
        await OnSave.InvokeAsync(true);
    }

    async Task CloseAsync() => await JsRuntime.InvokeVoidAsync("Modal.Close", "modal_new_category");

    async Task EnterKeyPressAsync(string keyPress)
    {
        if (keyPress == "Enter")
            await SaveAsync();
    }
}